import { render, screen, act } from '@testing-library/react'; import { describe, it, expect, vi } from 'vitest'; import App from './App';  vi.mock('./supabaseClient', () => ({     supabase: {         auth: {             getSession: vi.fn().mockResolvedValue({ data: { session: null }, error: null }),             onAuthStateChange: vi.fn().mockReturnValue({ data: { subscription: { unsubscribe: vi.fn() } } }),         },         from: vi.fn().mockReturnThis(),         select: vi.fn().mockReturnThis(),         eq: vi.fn().mockReturnThis(),         single: vi.fn().mockResolvedValue({ data: null, error: null }),     }, }));  vi.mock('framer-motion', () => {     const Div = ({ children, ...props }) => <div {...props}>{children}</div>;     return {         motion: new Proxy({}, { get: () => Div }),         AnimatePresence: ({ children }) => <>{children}</>,     }; });  vi.mock('./hooks/useNotifications', () => ({     default: () => ({ unreadMessages: 0, unreadAnnouncements: 0 }), }));  describe('App Component', () => {     it('renders the welcome page when unauthenticated', async () => {         await act(async () => {             render(<App />);         });          const cgbElements = await screen.findAllByText(/CGB/i);         expect(cgbElements.length).toBeGreaterThan(0);         const loginBtn = await screen.findByText(/Iniciar Sesión/i);         expect(loginBtn).toBeInTheDocument();     }); });